<Window x:Class="SomewhereDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SomewhereDesktop"
        mc:Ignorable="d"
        Title="Somewhere Desktop"
        WindowState="Normal" WindowStyle="None" AllowsTransparency="False"
        Closing="Window_Closing" ResizeMode="CanResizeWithGrip" MouseDown="Window_MouseDown"
        Background="{StaticResource Base0}"
        DataContext="{Binding RelativeSource={RelativeSource self}}">
    <Window.CommandBindings>
        <!-- Window Commands -->
        <CommandBinding Command="Close" CanExecute="CloseWindowCommand_CanExecute" Executed="CloseWindowCommand_Executed"/>
        <CommandBinding Command="Zoom" CanExecute="MaximizeWIndowCommand_CanExecute" Executed="MaximizeWindowCommand_Executed"/>
        <CommandBinding Command="BrowseBack" CanExecute="HideWindowCommand_CanExecute" Executed="HideWindowCommand_Executed"/>
        <!-- Repository Commands -->
        <CommandBinding Command="Open" CanExecute="OpenHomeCommand_CanExecute" Executed="OpenHomeCommand_Executed"/>
        <!-- Inventory Commands -->
        <CommandBinding Command="Find" CanExecute="SearchCommand_CanExecute" Executed="SearchCommand_Executed"/>
        <!-- Notebook Commands -->
        <CommandBinding Command="New" CanExecute="CreateNoteCommand_CanExecute" Executed="CreateNoteCommand_Executed"/>
    </Window.CommandBindings>
    <Window.InputBindings>
        <!-- Window Commands -->
        <KeyBinding Key="F11" Command="Zoom"/>
        <!-- Repository Commands -->
        <KeyBinding Key="F1" Command="Open"/>
        <!-- Inventory Commands -->
        <KeyBinding Key="F3" Command="Find"/>
        <KeyBinding Key="Esc" Command="BrowseBack"/>
        <!-- Notebook Commands -->
        <KeyBinding Key="F2" Command="New"/>
    </Window.InputBindings>
    <Grid>
        <DockPanel LastChildFill="True">
            <Grid Background="{StaticResource Base1}" DockPanel.Dock="Top">
                <StackPanel Orientation="Horizontal">
                    <Label x:Name="InventoryTabLabel" Content="Inventory" Style="{StaticResource Title}" PreviewMouseDown="TabHeader_MouseDown"/>
                    <Label x:Name="NotebookTabLabel" Content="Notebook" Style="{StaticResource TitleDim}" PreviewMouseDown="TabHeader_MouseDown"/>
                    <Label x:Name="KnowledgeTabLabel" Content="Knowledge" Style="{StaticResource TitleDim}" PreviewMouseDown="TabHeader_MouseDown"/>
                    <Label x:Name="LogsTabLabel" Content="Logs" Style="{StaticResource TitleDim}" PreviewMouseDown="TabHeader_MouseDown"/>
                    <Label x:Name="NTFSSearchTabLabel" Content="NTFSSearch" Style="{StaticResource TitleDim}" PreviewMouseDown="TabHeader_MouseDown"/>
                    <Label x:Name="StatusTabLabel" Content="Status" Style="{StaticResource TitleDim}" PreviewMouseDown="TabHeader_MouseDown"/>
                    <Label x:Name="SettingsTabLabel" Content="Settings" Style="{StaticResource TitleDim}" PreviewMouseDown="TabHeader_MouseDown"/>
                </StackPanel>
                <DockPanel HorizontalAlignment="Right">
                    <Button Content="➖" Width="35" Height="35" Command="BrowseBack"/>
                    <Button Content="▣" Width="35" Height="35" Command="Zoom"/>
                    <Button Content="✖" Width="35" Height="35" Command="Close"/>
                </DockPanel>
            </Grid>
            <Grid>
                <DockPanel LastChildFill="True">
                    <!-- Info Bar -->
                    <DockPanel DockPanel.Dock="Bottom" LastChildFill="True">
                        <Label Content="{Binding InfoText}" Style="{StaticResource Info}"/>
                    </DockPanel>
                    <!-- Tabs -->
                    <Grid>
                        <!-- Items Panel -->
                        <DockPanel x:Name="InventoryPanel" Visibility="Visible">
                            <!-- Items List Region -->
                            <Border Style="{StaticResource Panel}">
                                <StackPanel>
                                    <DockPanel>
                                        <Label Content="Search Name"/>
                                        <TextBox Text="{Binding SearchNameKeyword, UpdateSourceTrigger=PropertyChanged}"/>
                                        <Label Content="Type Filter"/>
                                        <ComboBox ItemsSource="{Binding TypeFilters}" SelectedItem="{Binding SelectedTypeFilter}"/>
                                    </DockPanel>
                                    <ListView ItemsSource="{Binding Items}" SelectionMode="Single"
                                              SelectedItem="{Binding ActiveItem}">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Label Content="{Binding DisplayName}" Style="{StaticResource Header}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </StackPanel>
                            </Border>
                            <!-- Tag Search Region -->
                            <Border Style="{StaticResource Panel}" DockPanel.Dock="Top">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Tag Filters"/>
                                        <ListView ItemsSource="{Binding TagFilters}" Style="{StaticResource WrapList}">
                                            <ListView.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Style="{StaticResource TagFilterButton}">
                                                        <TextBlock Text="{Binding}" Style="{StaticResource TagFilter}" MouseDown="RemoveTagFilter_MouseDown"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                        </ListView>
                                    </StackPanel>
                                    <DockPanel LastChildFill="True">
                                        <Label Content="Search Tag"/>
                                        <TextBox Text="{Binding SearchTagKeyword, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DockPanel>
                                    <DockPanel>
                                        <Label Content="Tags" Style="{StaticResource Header}"/>
                                        <ListView ItemsSource="{Binding SearchTags}" Style="{StaticResource WrapList}">
                                            <ListView.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Style="{StaticResource TagButton}">
                                                        <!-- Use TextBlock to avoid underscore be escaped as access key -->
                                                        <TextBlock Text="{Binding}" Style="{StaticResource Tag}" MouseDown="AddTagFilter_MouseDown"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                        </ListView>
                                    </DockPanel>
                                </StackPanel>
                            </Border>
                            <DockPanel LastChildFill="True">
                                <!-- Actions Region -->
                                <Border Style="{StaticResource Panel}" DockPanel.Dock="Bottom">
                                    <DockPanel>
                                        <StackPanel>
                                            <Button Content="Open" Command="Open"/>
                                        </StackPanel>
                                        <StackPanel>

                                        </StackPanel>
                                    </DockPanel>
                                </Border>
                                <!-- Item Preview Region -->
                                <Grid DockPanel.Dock="Top">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="7*"/>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition Width="3*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Style="{StaticResource Panel}" Grid.Column="0">
                                        <!-- Preview Area -->
                                    </Border>
                                    <GridSplitter Grid.Column="1"/>
                                    <Border Style="{StaticResource Panel}" Grid.Column="2">
                                        <DockPanel>
                                            <DockPanel DockPanel.Dock="Top">
                                                <Label Content="Name" />
                                                <TextBox IsReadOnly="{Binding IsItemFieldReadOnly}" IsReadOnlyCaretVisible="{Binding IsItemFieldReadOnly}" Text="{Binding ActiveItemName}"/>
                                            </DockPanel>
                                            <DockPanel DockPanel.Dock="Top">
                                                <Label Content="Tags"/>
                                                <TextBox IsReadOnly="{Binding IsItemFieldReadOnly}" IsReadOnlyCaretVisible="{Binding IsItemFieldReadOnly}" Text="{Binding ActiveItemTags}"/>
                                            </DockPanel>
                                            <TextBox IsReadOnly="{Binding IsItemFieldReadOnly}" IsReadOnlyCaretVisible="{Binding IsItemFieldReadOnly}" Text="{Binding ActiveItemMeta}" Style="{StaticResource Content}"/>
                                        </DockPanel>
                                    </Border>
                                </Grid>
                            </DockPanel>
                        </DockPanel>
                        <!-- Notebook Panel -->
                        <DockPanel x:Name="NotebookPanel" Visibility="Collapsed">
                            <Border Style="{StaticResource Panel}" DockPanel.Dock="Top">
                                <DockPanel>
                                    <Label Content="Search" DockPanel.Dock="Left"/>
                                    <TextBlock DockPanel.Dock="Bottom">
                                Enter tags, names, and keywords, seperated by spaces, to search all existing notes.
                                    </TextBlock>
                                    <Button Content="Export All" Width="100" DockPanel.Dock="Right" Margin="2, 0, 2, 0"/>
                                    <Button Content="Cancel" Width="100" DockPanel.Dock="Right" Margin="2, 0, 2, 0"/>
                                    <TextBox DockPanel.Dock="Left"/>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource Panel}">
                                <DockPanel>
                                    <Button DockPanel.Dock="Bottom" Content="Create Note" Command="New"/>
                                    <ListView ItemsSource="{Binding Notes}" SelectionMode="Single"
                                              SelectedItem="{Binding ActiveNote}">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Label Content="{Binding DisplayName}" Style="{StaticResource Header}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </DockPanel>
                            </Border>
                            <Border Style="{StaticResource Panel}">
                                <DockPanel>
                                    <DockPanel DockPanel.Dock="Top">
                                        <Label Content="Name" />
                                        <TextBlock DockPanel.Dock="Bottom">
                                    Enter the name of the note; Name can be anything that helps you remember.
                                    <LineBreak/>
                                    It's recommended to use a format like "[Category] Identifying Name" to help organize notes.
                                        </TextBlock>
                                        <TextBox IsEnabled="{Binding IsNoteFieldEditEnabled}" Text="{Binding ActiveNoteName}"/>
                                    </DockPanel>
                                    <DockPanel DockPanel.Dock="Top">
                                        <Label Content="Tags"/>
                                        <TextBlock DockPanel.Dock="Bottom">
                                    Enter tags for the note, seperated by comma.
                                    <LineBreak/>
                                    Tags are used to organize notes and help quickly search for note.
                                        </TextBlock>
                                        <TextBox IsEnabled="{Binding IsNoteFieldEditEnabled}" Text="{Binding ActiveNoteTag}"/>
                                    </DockPanel>
                                    <TextBox IsEnabled="{Binding IsNoteFieldEditEnabled}" Text="{Binding ActiveNoteContent}" Style="{StaticResource Content}"/>
                                </DockPanel>
                            </Border>
                        </DockPanel>
                        <!-- Knowledge Panel -->
                        <DockPanel x:Name="KnowledgePanel" Visibility="Collapsed">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource Panel}" Grid.Column="0">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="8*"/>
                                            <RowDefinition Height="135"/>
                                            <RowDefinition Height="2*"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="65"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Region Labels -->
                                        <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource KnowledgeRegionHighlight}">
                                            <Label Content="Up" Style="{StaticResource KnowledgeRegionMark}"/>
                                        </Border>
                                        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource KnowledgeRegionHighlight}">
                                            <Label Content="Current"  Style="{StaticResource KnowledgeRegionMark}"/>
                                        </Border>
                                        <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource KnowledgeRegionHighlight}">
                                            <Label Content="Down" Style="{StaticResource KnowledgeRegionMark}"/>
                                        </Border>
                                        <!-- Region Tags and Texts -->
                                        <ListView Grid.Row="0" Grid.Column="1"/>
                                        <TextBox IsReadOnly="True" Grid.Row="1" Grid.Column="1"/>
                                        <DockPanel LastChildFill="True"  Grid.Row="2" Grid.Column="1">
                                            <!-- Tags List -->
                                            <ListView/>
                                            <!-- Notes List -->
                                            <ListView/>
                                        </DockPanel>
                                    </Grid>
                                </Border>
                                <GridSplitter Grid.Column="1"/>
                                <Border Style="{StaticResource Panel}" Grid.Column="2">
                                    
                                </Border>
                            </Grid>
                        </DockPanel>
                        <!-- Logs Panel -->
                        <DockPanel x:Name="LogsPanel" Visibility="Collapsed">
                            <DockPanel LastChildFill="True">
                                <TextBox IsReadOnly="True" Text="{Binding LogsText}"/>
                            </DockPanel>
                        </DockPanel>
                        <!-- NTFS Search Panel -->
                        <DockPanel x:Name="NTFSSearchPanel" Visibility="Collapsed">
                            <DockPanel LastChildFill="True">
                            </DockPanel>
                        </DockPanel>
                        <!-- Status Panel -->
                        <DockPanel x:Name="StatusPanel" Visibility="Collapsed" LastChildFill="True">
                            <Button Content="Add All" Click="RepositoryAddAllFiles_Click" DockPanel.Dock="Bottom"/>
                            <TextBox IsReadOnly="True" Text="{Binding StatusText}"/>
                        </DockPanel>
                        <!-- Settings Panel -->
                        <DockPanel x:Name="SettingsPanel" Visibility="Collapsed">
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Top">
                            To edit settings, please use <Bold>CLI (Command Line Interface)</Bold> command `<Italic>config</Italic>`, 
                            or use <Bold>SQLite DB Browser</Bold> to edit <Italic>Configuration</Italic> table directly.
                                </TextBlock>
                                <TextBox IsReadOnly="True" Text="{Binding ConfigurationsText}"/>
                            </DockPanel>
                        </DockPanel>
                    </Grid>
                </DockPanel>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
